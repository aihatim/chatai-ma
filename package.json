{
  "name": "chatai-ma",
  "version": "0.1.0",
  "private": true,
  "description": "ChatAi.ma — Enterprise AI Revenue Engine",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {}
}
